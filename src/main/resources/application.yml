spring:
  application:
    name: api-nextage

  datasource:
    url: jdbc:postgresql://localhost:5433/taskmanager
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update